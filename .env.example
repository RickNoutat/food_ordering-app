# Copie ce fichier en .env et remplis les valeurs
# Ne JAMAIS committer .env dans git

# ─── Appwrite ─────────────────────────────────────────────────────────────────
EXPO_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
EXPO_PUBLIC_APPWRITE_PROJECT_ID=your_project_id
EXPO_PUBLIC_APPWRITE_DATABASE_ID=your_database_id
EXPO_PUBLIC_APPWRITE_COLLECTION_ID=your_collection_id

# ─── Sentry ───────────────────────────────────────────────────────────────────
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# ─── Codemagic (définis dans l'interface, pas ici) ────────────────────────────
# EXPO_TOKEN=            → token EAS (expo.dev > Access Tokens)
# EMAIL_RECIPIENT=       → email pour les notifications de build
# NETLIFY_AUTH_TOKEN=    → optionnel pour le déploiement web
# NETLIFY_SITE_ID=       → optionnel pour le déploiement web
